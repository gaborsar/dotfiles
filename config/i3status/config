general {
        colors = false
}

order += "disk /home"
order += "ethernet _first_"
order += "wireless _first_"
order += "battery all"
order += "volume master"
order += "tztime local"

disk "/home" {
        format = " %avail"
}

ethernet _first_ {
        format_up = " %speed"
        format_down = " down"
}

wireless _first_ {
        format_up = " %quality"
        format_down = " down"
}

battery all {
        status_chr = ""
        status_bat = ""
        status_unk = "?"
        status_full = ""
        format = "%status %percentage"
        hide_seconds = true
}

volume master {
        format = " %volume"
        format_muted = " %volume"
        device = "default"
        mixer = "Master"
        mixer_idx = 0
}

tztime local {
        format = " %d %b %H:%M"
}
